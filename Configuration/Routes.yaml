#                                                                        #
# Routes configuration                                                   #
#                                                                        #
# This file contains the configuration for the MVC router.               #
# Just add your own modifications as necessary.                          #
#                                                                        #
# Please refer to the Flow manual for possible configuration options.    #
#                                                                        #

-
  name: 'app'
  uriPattern: 'app/{@action}'
  defaults:
    '@package': 'AchimFritz.Documents'
    '@controller': 'App14'

-
  name: 'app sub'
  uriPattern: 'app/{action}/{sub}'
  defaults:
    '@package': 'AchimFritz.Documents'
    '@controller': 'App14'
    '@action': 'index'

-
  name: 'app sub sub'
  uriPattern: 'app/{action}/{sub}/{subsub}'
  defaults:
    '@package': 'AchimFritz.Documents'
    '@controller': 'App14'
    '@action': 'index'

-
  name: 'app sub sub sub'
  uriPattern: 'app/{action}/{sub}/{subsub}/{subsubsub}'
  defaults:
    '@package': 'AchimFritz.Documents'
    '@controller': 'App14'
    '@action': 'index'

-
  name: 'app sub sub sub sub'
  uriPattern: 'app/{action}/{sub}/{subsub}/{subsubsub}/{subsubsubsub}'
  defaults:
    '@package': 'AchimFritz.Documents'
    '@controller': 'App14'
    '@action': 'index'

-
  name: 'app sub sub sub'
  uriPattern: 'app/{action}/{sub}/{subsub}/{subsubsub}/{subsubsubsub}/{subsubsubsubsub}'
  defaults:
    '@package': 'AchimFritz.Documents'
    '@controller': 'App14'
    '@action': 'index'